# HubSpot Private App token (crm.objects.contacts.read + crm.objects.deals.read + crm.owners.read)
# Ottienilo da: HubSpot > Settings > Integrations > Private Apps
HUBSPOT_API_KEY=pat-eu1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Vercel Environment Variable name (per deploy):
# Aggiungi come "hubspot_api_key" in Vercel > Project Settings > Environment Variables
